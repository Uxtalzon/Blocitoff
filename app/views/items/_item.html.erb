<%= content_tag(:li, id: "item-#{item.id}") do %>
<%= item.name %> <em><strong><%= item.days_left %> days until deletion</strong></em> <%= link_to "", item, method: :delete, class: 'glyphicon glyphicon-ok', remote: true %>
<% end %>